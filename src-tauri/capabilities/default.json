{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the Zero app shell windows",
  "windows": ["tray", "main", "preferences", "about", "capture", "pin-*", "launcher", "paper"],
  "permissions": [
    "core:default",
    "autostart:default",
    "global-shortcut:default",
    "opener:default"
  ]
}
